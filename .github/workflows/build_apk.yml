name: Build Android APK (Vite + Capacitor)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Project Dependencies
        run: |
          npm install
          npm install @tailwindcss/oxide-linux-x64-gnu @tailwindcss/vite

      - name: Build Web Assets (Vite)
        run: |
          npm run build

      - name: Setup Capacitor & Add Android
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init "YolAcik" "com.yolacik.app" --web-dir "dist" || true
          npx cap add android || true
          npx cap sync android

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Fix BouncyCastle Version (if needed)
        run: |
          if [ -f android/build.gradle ]; then
            echo -e "\nallprojects { configurations.all { resolutionStrategy { force 'org.bouncycastle:bcprov-jdk18on:1.78' } } }" >> android/build.gradle
          fi

      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: YolAcik-Debug-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
